node_modules/
dist/
# Commit the built frontend so Render can serve it without running vite.
!client/dist/
!client/dist/**
# Duplicate asset caches inside dist stay ignored.
client/dist/duplicated.assets/
.env
*.log
*.pid
.DS_Store

# Uploaded documents
server/uploads/

# Play Store release signing secrets
PLAY_STORE/keys/
client/android/keystore.properties

# Runtime / transient artifacts
server/src/.otp-store.json
client/android/build-log.txt
